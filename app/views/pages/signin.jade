doctype
html
  head
    meta(http-equiv=Content-Type content="text/html;charset=utf-8")
    meta(name="baidu-site-verification" content="I8jGrwezyY")
    meta(http-equiv="X-UA-Compatible" content="IE=edge,chrome=1")
    meta(name="Keywords" content="软通盛典,nodejs,mongodb,bootstrap,慕课网")
    meta(name= "description"   content="重庆邮电大学,记录大学时光的精彩")
    title #{title}
    style(.body{padding-top: 130px;})
    include ../includes/head
  body
    include ../includes/header
    div(class="container" style="margin-top:120px;")
      .row
        .page-header
          h1 #{title}
        form(method="POST",action="/user/signin")
          div(class="col-md-6")
            div(class="form-group")
              //-文字和控件绑定的label标签
              label(for="signinName") 用户名
              input(id="signinName" class="form-control" name="user[name]" type="text")
            div(class="form-group ")
              label(for="signinPassword") 密码
              input(id="signinPassword" class="form-control" name="user[password]" type="password")
            div(class="form-group")
              button(class="btn btn-default" type="reset") reset
              button(class="btn btn-success",type="submit") 登录
            h4.text-info 还没有账户？
              a(href="/signup" class="text-danger") 立即注册

        div(class="col-md-6")
          h5.text-danger 普通用户和未登录用户
            ul
              li 没有管理员权限，无法对数据操纵
              li 未登录的游客无法对文章进行评论
          h5.text-info 目前提供管理员帐号
            ul
              li 用户名:hqy
              li 密码:qq123456
              li 用户名:ttt
              li 密码:123
          h5.text-primary 如果您只想实现评论和回复功能，请注册一个帐号即可
      //-
        form(method="POST",action="/user/signin")
          .modal-header 登录
          .modal-body
            .form-group
              //-文字和控件绑定的label标签
              label(for="signinName") 用户名
              input(id="signinName" class="form-control" name="user[name]" type="text")
            .form-group
              label(for="signinPassword") 密码
              input(id="signinPassword" class="form-control" name="user[password]" type="password")
          .modal-footer
            button(class="btn btn-default" type="button",data-dismiss="modal") 关闭
            button(class="btn btn-success",type="submit") 登录
